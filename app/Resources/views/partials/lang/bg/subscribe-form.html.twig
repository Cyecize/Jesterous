<form action="{{ path('subscribe_global') }}" method="post">
    <div class="input-group">
        <input class="btn btn-lg" name="email" id="email" type="email" placeholder="Вашия Email адрес" required>
        <input type="hidden" name="token" value="{{ csrf_token('token') }}">
        <button class="sub-btn btn btn-info btn-lg" type="submit">Абониране</button>
    </div>
</form>
<div class="promise">Няма да спамим!</div>
